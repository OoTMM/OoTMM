SECTIONS
{

#ifdef GAME_OOT
# define BASE_ADDR 0x80400000
# include "link_oot.in"
#endif

#ifdef GAME_MM
# define BASE_ADDR 0x80760000
# include "link_mm.in"
#endif

  . = BASE_ADDR;
  .text : {
    *(.text*);
    *(.data*);
    *(.rodata*);
    *(.bss*);
  }

  . = 0x80800000;
  .patch : {
    *(.patch);
  }
  /DISCARD/ : { *(.*); }
}
