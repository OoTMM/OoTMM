{
  "name": "@ootmm/web-components",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "tsx ./build.ts"
  },
  "author": "Nax",
  "license": "MIT",
  "dependencies": {
    "@ootmm/gui": "*"
  },
  "nx": {
    "targets": {
      "build": {
        "inputs": [
          "{projectRoot}/build.ts"
        ],
        "outputs": [
          "{projectRoot}/dist/**/*"
        ],
        "dependsOn": [
          "^build"
        ]
      }
    }
  }
}
