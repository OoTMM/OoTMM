{
  "name": "@ootmm/data",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.ts",
  "scripts": {
    "build": "tsx src/build.ts"
  },
  "author": "Nax",
  "license": "MIT",
  "devDependencies": {
    "yaml": "^2.3.4",
    "csv": "^6.3.6"
  },
  "nx": {
    "targets": {
      "build": {
        "inputs": [
          "{projectRoot}/**/*"
        ],
        "outputs": [
          "{workspaceRoot}/dist"
        ],
        "dependsOn": [
          "^build"
        ]
      }
    }
  }
}
