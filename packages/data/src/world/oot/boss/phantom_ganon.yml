"Forest Temple Boss":
  boss: true
  age_change: false
  dungeon: Forest
  events:
    BOSS_PHANTOM_GANON: "soul_boss(SOUL_BOSS_PHANTOM_GANON) && (has_ranged_weapon_adult || can_use_slingshot) && (can_use_sword || has_bottle || can_boomerang) && (can_use_sword_or_sticks || can_hammer)"
  exits:
    "Forest Temple After Boss": "event(BOSS_PHANTOM_GANON)"

"Forest Temple After Boss":
  boss: true
  age_change: false
  dungeon: Forest
  locations:
    "Forest Temple Boss": "true"
    "Forest Temple Boss Container": "true"
