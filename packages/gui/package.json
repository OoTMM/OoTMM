{
  "name": "@ootmm/gui",
  "version": "0.0.1",
  "description": "The graphical OoTMM generator widget",
  "type": "module",
  "main": "src/app/index.ts",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build && tsx ./post-build.ts",
    "build:desktop": "TARGET=desktop vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OoTMM/gui.git"
  },
  "author": "Nax",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/OoTMM/gui/issues"
  },
  "homepage": "https://github.com/OoTMM/gui#readme",
  "dependencies": {
    "@ootmm/generator": "*",
    "clsx": "^2.1.1",
    "jszip": "^3.10.1",
    "lodash-es": "catalog:",
    "preact": "catalog:",
    "react-ga4": "^3.0.1",
    "react-icons": "catalog:",
    "tailwind-merge": "^3.5.0",
    "tailwindcss": "catalog:",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@types/lodash-es": "catalog:",
    "rollup-plugin-visualizer": "^7.0.1"
  },
  "nx": {
    "namedInputs": {
      "default": [
        "{projectRoot}/**/*"
      ],
      "env": [
        {
          "env": "VERSION"
        },
        {
          "env": "VERSION_SUFFIX"
        },
        {
          "env": "STATIC_URL"
        }
      ]
    },
    "targets": {
      "build": {
        "inputs": [
          "default",
          "env"
        ],
        "outputs": [
          "{workspaceRoot}/dist/web"
        ]
      },
      "build:desktop": {
        "inputs": [
          "default",
          "env"
        ],
        "outputs": [
          "{workspaceRoot}/dist/desktop"
        ]
      },
      "dev": {
        "dependsOn": [
          "^build"
        ]
      }
    }
  }
}
